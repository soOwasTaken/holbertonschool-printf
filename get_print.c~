int (*get_print(char s))(va_list, list)
{
	   func_arr[] = {
		{'s', print_string},
		{'c', print_char},
		{'%', print_percent}
		};
	int flags = 14;

	register int i;

	for (i = 0; i < flags; i++)
		if (func_arr[i].c == s)
			return (func_arr[i].f);
	return (NULL);
}
